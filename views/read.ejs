<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900">
    <div class="nav text-blue-500 mb-10">
        <a href="/">Back to Home page</a>
    </div>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <h1 class="text-3xl tracking-tighter mb-3">All users</h1>
        <% users.forEach(function(user){ %>
            <div class="user w-72 bg-zinc-800 rounded-lg mb-6 float-left mr-6 p-5">
                <div class="w-full h-52 rounded-lg bg-zinc-800 overflow-hidden">
                    <img class="w-full h-full object-cover" src="<%= user.imageurl %>" alt="">
                </div>
                <h3 class="text-xl text-white mt-3 tracking-tighter"><%= user.username %></h3>
                <h5 class="text-zinc-500"><%= user.email %></h5>
                <div class="flex gap-3 mt-10">
                    <a class="text-zinc-500" href="">Edit User</a>
                    <a class="text-red-500" href="/delete/<%=user._id %>">Delete user</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>